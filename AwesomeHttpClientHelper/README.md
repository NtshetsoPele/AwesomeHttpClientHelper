# AwesomeHttpClientHelper

A reusable HTTP client helper with retry, logging, JSON, and bearer token support for .NET 9.

## Features
- GET and POST with automatic JSON serialization/deserialization
- Retry policy using Polly
- Optional Bearer token authentication
- Logging support
- Production-ready HTTP client helper

## Installation
```bash
dotnet add package AwesomeHttpClientHelper